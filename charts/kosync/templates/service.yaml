apiVersion: v1
kind: Service
metadata:
  name: {{ include "kosync.fullname" . }}
  labels:
    {{- include "kosync.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kosync.selectorLabels" . | nindent 4 }}
